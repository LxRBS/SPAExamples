正确引用LLVM外部库，此例中LLVM版本无所谓

正确配置好CMakeLists.txt内容

在build目录下依次执行
cmake ..
make


然后将
将eg0201的if.c和for.c拷贝到build目录
依次执行
lxllvm if.c
lxllvm for.c

再执行
./bin/main if
./bin/main for

即可观察到if.ll和for.ll中关于模块、函数和BB的内容。

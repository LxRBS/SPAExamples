使用z3模拟程序验证。

一个swap小程序
```cpp
a = a + b
b = a - b
a = a - b
```
写成SSA

```cpp
a0 = a;
b0 = b;
a1 = a0 + b0;
b1 = a1 - b0;
a2 = a1 - b1;
b2 = b1
```

写成逻辑命题

a0==a && b0==b && a1==b0+b0 && b1==a1-b0 && a2==a1-b1 && b2==b1 implies a2==b && b2==a

这个命题永真，即该程序的交换功能在任何情况下均正确。
注意到这里没有考虑整型，而是使用了数学上的整数概念。